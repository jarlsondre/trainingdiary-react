# Loaded automatically by Create React App during `npm start` (development only).
# Production builds (Netlify's `npm run build`) do NOT read this file and fall
# back to the production API URL hardcoded in src/http-common.ts.
REACT_APP_API_URL=http://localhost:8000
